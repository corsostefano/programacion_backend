const Contenedor = require("./contenedor.js");

const contenedor = new Contenedor();

 /*contenedor
  .save({ title: "Sansung Galaxy", price: 158000, thumbnail: "url" })
  .then((result) => {
    console.log(result.message);
  });*/

/* contenedor
  .save({ title: "Camara Nikkon", price: 250000, thumbnail: "url" })
  .then((result) => {
    console.log(result.message);
  });*/ 

/*contenedor
  .save({ title: "Playstation 5", price: 280000, thumbnail: "url" })
  .then((result) => {
    console.log(result.message);
  });*/ 

/*contenedor.getById(3).then((result) => {
  console.log(result.product);
});*/ 

/* contenedor.getAll().then((result) => {
  console.log(result.products);
}); */

/*contenedor.deleteById(2).then((result) => {
  console.log(result.message);
}); */

/* contenedor.deleteAll(); */








